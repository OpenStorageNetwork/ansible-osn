Ansible 2.6.5
Python 2.7.5

pip install --user --ignore-installed ansible netaddr

Add host names or addresses to inventory.

ANSIBLE_HOST_KEY_CHECKING=False ansible-playbook site.yml

#ceph-volume lvm create --bluestore --data /dev/xvdb
#
#systemctl start ceph-osd@0
#
#/var/lib/ceph/osd/ceph-1/
